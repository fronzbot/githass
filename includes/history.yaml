# For history, want to keep database to manageable size
# so dump data after 7 days and only track useful
# information (ie. don't care about light/switch stuff)

exclude:
  entities:
    - sun.sun
    - sensor.date
