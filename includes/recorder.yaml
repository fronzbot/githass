db_url: !secret db_url
purge_interval: 1
purge_keep_days: 2
commit_interval: 2
exclude:
  domains:
    - automation
    - camera
    - media_player
    - scene
    - script
  entities:
    - sun.sun
    - sensor.date_time
